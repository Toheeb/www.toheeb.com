
<footer>
    <nav>
      <h2>Pagination</h2>
      
      {% set hc = collections['book-hc-page-list'] %}
      {% set previousPost = hc | getPreviousCollectionItem %}
      {% set nextPost = hc | getNextCollectionItem %}
      {% set currentPost = hc | getCollectionItem %}

      <p>
        {{currentPost.data.title}} is the Page {{ currentPost.data["book-hc"] }} of <a href="/en/html-by-contents/"> <cite>HTML by Contents</cite> </a>
      </p>
      

      <ol>
        {% if previousPost %} 
        <li>
          <p>
            Previous Page: <a href="{{ previousPost.url }} "> {{ previousPost.data.title }}.</a> 
          </p>
        </li>
        {% endif %}

        {% if nextPost %} 
        <li>
          <p>
            Next Page: <a href="{{ nextPost.url }} "> {{ nextPost.data.title }}.</a> 
          </p>
        </li>
        {% endif %}
      </ol>

    </nav>

  </footer>